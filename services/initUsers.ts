// User initialization is now handled by the backend via `npm run seed`.
// These functions are kept as no-ops for backwards compatibility.

export function initializeDefaultUsers() {}
export function ensureAdminStaffAccounts() {}
